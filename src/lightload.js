var lightload = (function() {
	/*
		metodos: 
		
			lightload.get(
				{url: 'mi-js.js', name: 'js'},
				{url: 'mi-css.css, cache: false, expires: 4}
			);
			
			// Si no se le asigna un nombre la url sera el nombre
			
			lightload.remove(name);
			lightload.clear();
	
	*/
	
	return {
		get: function(urls) {
		
		},
		remove: function(name) {
		
		},
		clear: function() {
		
		}
	}
})();